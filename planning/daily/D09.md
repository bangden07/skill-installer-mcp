# Daily Log - D09

## Focus

Fokus utama hari ini:
- wire real MCP SDK ke server (ganti placeholder)
- ganti Groq reranker dengan OpenRouter reranker
- sinkronkan backlog dengan status implementasi aktual

## Planned Tasks

- [x] install `@modelcontextprotocol/sdk`
- [x] rewrite `src/mcp/server.ts` dengan `McpServer` + `StdioServerTransport`
- [x] verifikasi 10 tools terdaftar dan merespons via JSON-RPC stdio
- [x] rename `useGroqRerank` ke `useRerank` di schema, recommender, dan handler
- [x] implement `src/recommender/openrouter-reranker.ts`
- [x] wire reranker ke `recommendSkills` flow
- [x] tambah 6 test baru untuk reranker (parse, fallback, edge cases)
- [x] update semua planning docs (Groq -> OpenRouter)
- [x] update `planning/backlog.md` — mark semua item yang sudah selesai
- [x] tambah DEC-015 dan DEC-016 ke `planning/decisions.md`

## Progress

Yang dikerjakan hari ini:
- menginstall `@modelcontextprotocol/sdk` v1.27.1 sebagai runtime dependency
- menulis ulang `src/mcp/server.ts` dari placeholder ke real MCP SDK wiring
- semua 10 tools didaftarkan via `server.tool()` dengan Zod schema `.shape`
- rename `useGroqRerank` ke `useRerank` di 3 file (schema, recommender, handler)
- implement `src/recommender/openrouter-reranker.ts` dengan fitur:
  - call OpenRouter `/chat/completions` API
  - parse response (plain JSON, code block, embedded array)
  - graceful fallback jika no API key, timeout, atau parse error
  - score adjustment: LLM rank dominant, rules score sebagai tiebreaker
- wire reranker ke `recommendSkills` — aktif jika `useRerank: true`
- tambah 6 test baru: parse plain JSON, parse code block, parse mixed text, invalid input, no-API-key fallback, single-candidate skip
- update semua planning docs dari Groq ke OpenRouter
- total tests: 35 (dari 29 sebelumnya)

## Decisions

Keputusan penting hari ini:
- DEC-015: menggunakan `@modelcontextprotocol/sdk` sebagai MCP runtime
- DEC-016: mengganti Groq dengan OpenRouter untuk reranker, env var `OPENROUTER_API_KEY`

## Problems

Masalah yang muncul:
- tidak ada masalah signifikan

## Mitigation

Cara mengatasi atau langkah lanjut:
- project MVP Tahap 1 secara fungsional sudah selesai termasuk P2 reranker

## Deliverables

Output yang selesai hari ini:
- update `src/mcp/server.ts` (real MCP SDK wiring)
- update `src/schema/tools.ts` (useGroqRerank -> useRerank)
- update `src/recommender/recommend-skills.ts` (wire reranker)
- update `src/mcp/tools/recommend-skills.ts` (useRerank)
- `src/recommender/openrouter-reranker.ts` (NEW)
- update `src/recommender/recommender.test.ts` (+6 tests)
- update `package.json` (tambah `@modelcontextprotocol/sdk`)
- update `planning/backlog.md`
- update `planning/decisions.md` (DEC-015, DEC-016)
- update `planning/architecture.md`
- update `planning/sprint-14-days.md`
- `planning/daily/D09.md`

## Status

Status akhir hari:
- `on track`

## Next Day

Target hari berikutnya:
- MVP Tahap 1 selesai — semua P0, P1, dan P2 items complete
- bisa mulai planning Tahap 2 jika diinginkan
