# Daily Log - D03

## Focus

Fokus utama hari ini:
- memperkuat schema dan state foundation
- mulai membangun adapter foundation dan 3 adapter pertama

## Planned Tasks

- [x] tambahkan `PlanStore`
- [x] tambahkan `LockStore`
- [x] tambahkan bootstrap files yang masih kurang
- [x] implement `BaseAgentAdapter`
- [x] implement `CursorAdapter`, `OpenCodeAdapter`, dan `CodexAdapter`

## Progress

Yang dikerjakan hari ini:
- menambahkan `src/state/plan-store.ts`
- menambahkan `src/state/lock-store.ts`
- menambahkan bootstrap minimal untuk `config`, `mcp`, dan `index`
- menambahkan `src/adapters/agents/base.ts` dan helper `_shared.ts`
- menambahkan 3 adapter pertama beserta registry awal
- memperbarui `planning/backlog.md` untuk item adapter yang sudah selesai

## Decisions

Keputusan penting hari ini:
- adapter registry awal dikembalikan sebagai `Partial<AgentAdapterRegistry>` sampai semua agent selesai
- `Cursor`, `OpenCode`, dan `Codex` project scope memakai `direct` ke canonical path
- global scope untuk 3 adapter awal memakai native export dengan preferensi `symlink`

## Problems

Masalah yang muncul:
- project belum siap typecheck penuh karena bootstrap dependency dan file config Node/pnpm belum lengkap
- beberapa flow adapter belum diuji lewat tests

## Mitigation

Cara mengatasi atau langkah lanjut:
- lanjut lengkapi adapter berikutnya sebelum wiring installer core
- tambahkan tests setelah adapter layer MVP awal selesai
- siapkan `package.json` dan `tsconfig.json` bila ingin mulai verifikasi compile secara nyata

## Deliverables

Output yang selesai hari ini:
- `src/state/plan-store.ts`
- `src/state/lock-store.ts`
- `src/config/paths.ts`
- `src/schema/json-schema.ts`
- `src/mcp/register-tools.ts`
- `src/mcp/server.ts`
- `src/index.ts`
- `src/adapters/agents/base.ts`
- `src/adapters/agents/_shared.ts`
- `src/adapters/agents/cursor.ts`
- `src/adapters/agents/opencode.ts`
- `src/adapters/agents/codex.ts`
- `src/adapters/agents/registry.ts`

## Status

Status akhir hari:
- `on track`

## Next Day

Target hari berikutnya:
- implement `ClaudeCodeAdapter`, `WindsurfAdapter`, dan `AmpAdapter`
- lanjut ke installer core `plan/install`
